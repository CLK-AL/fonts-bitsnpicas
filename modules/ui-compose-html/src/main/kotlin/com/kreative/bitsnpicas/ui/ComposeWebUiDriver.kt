package com.kreative.bitsnpicas.ui

/**
 * Compose Web (wasmJs) rendering driver — scaffold / stub.
 *
 * S6 web sprint will implement Canvas2D-backed glyph rendering.
 * Until then this file serves as the module's compilation anchor.
 */
// Stub: UiDriver actual for wasmJs will be implemented in S6.
// The expect/actual pair requires a wasmJs source set in ui-shared
// (to be added when the wasmJs target is enabled on that module).
